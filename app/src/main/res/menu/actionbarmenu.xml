<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    style="@style/HeaderBar">

    <!-- News -->
    <item android:id="@+id/action_news"
        android:title="@string/news"
        app:showAsAction="always"/>

    <!-- Recipes -->
    <item android:id="@+id/action_recipes"
        android:title="@string/recipes"
        app:showAsAction="always"/>

    <!-- Currency -->
    <item android:id="@+id/action_currencies"
            android:title="@string/currencies"
            app:showAsAction="always"/>

    <!-- Station Finder -->
    <item android:id="@+id/action_stations"
        android:title="@string/stations"
        app:showAsAction="always"/>

</menu>